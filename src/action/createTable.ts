/**
 * 想法是通过在项目中定义一个model.js文件
 * 然后生成table 和 一些简单的查询表单
 * 
 * 主要问题 由于读取文件获取到的是一个字符串, 如何根据字符串解析出对应的字段
 */

// export function 






/**
 * 命令应该可以接受2个参数, 
 * 1. model的路径
 * 2. 生成文件的位置
 * 3. 生成文件的名字, 可以从路径当中解析, 如果路径的最后不存在 , 作为生成的文件名
 */
// const argv = process.argv;